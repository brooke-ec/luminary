{
  "name": "@luminary/monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm --parallel -r build",
    "check": "pnpm --parallel -r check"
  },
  "volta": {
    "node": "22.17.0"
  }
}
